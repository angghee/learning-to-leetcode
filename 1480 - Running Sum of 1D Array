class Solution:
    def runningSum(self, nums: List[int]) -> List[int]:
        length = len(nums)
        total = 0
        sum_list = []
        for i in range(length):
            total += nums[i]
            sum_list.append(total)
        return sum_list
        
